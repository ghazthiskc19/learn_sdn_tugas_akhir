# Mesh Experiments

This folder is reserved for `topology-mesh.py` experiments.

## Status

Mesh-specific experiments live here and are kept separate from the hierarchy workflow.

## Available Scripts

- `experiment_convergence_route.py` - route convergence time on the mesh topology
- `experiment_latency_e2e.py` - end-to-end latency on the mesh topology
- `experiment_throughput.py` - throughput on the mesh topology
- `experiment_hop_count.py` - hop count on the mesh topology

## Next Step

Add future mesh-specific metrics and scenarios here without mixing them with `scripts/experiments/hierarchy/`.